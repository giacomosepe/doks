backend:
  name: git-gateway
  branch: master # Branch to updamaste (optional; defaults to master)
media_folder: layout/sstatic
public_folder: layouts
collections:
  - name: 'blog'
    label: 'Blog'
    folder: 'content/blog'
    create: true
    slug: '{{year}}-{{month}}-{{day}}-{{slug}}'
    editor:
      preview: false
    fields:
      - { label: 'Title', name: 'title', widget: 'string' }
      - { label: 'Publish Date', name: 'date', widget: 'datetime' }
      - { label: 'Description', name: 'description', widget: 'string' }
      - { label: 'Body', name: 'body', widget: 'markdown' }
  - name: 'docs'
    label: 'Docs'
    folder: 'content/docs'
    create: true
    slug: '{{slug}}'
    preview: false
    fields: 
      - { label: 'Title', name: 'title', widget: 'string' }
      - { label: 'Topic', name: 'topic', widget: 'string' }
      - { label: 'Description', name: 'description', widget: 'string' }
      - { label: 'Body', name: 'body', widget: 'markdown' }
